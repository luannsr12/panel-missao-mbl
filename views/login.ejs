
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <link rel="icon" href="/images/favicon.ico" type="image/png">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/css/global.css">
    <link rel="stylesheet" href="/css/login.css">

    <%- include('partials/libs') %>
    
</head>
<body class="login-page">
    <img class="logo-login" alt="" src="/images/logo-missao.png">
    <div class="card login-card">
        <div class="card-body">
            <h3 class="login-title">BEM-VINDO DE VOLTA</h3>
            <% if (error) { %>
                <div class="alert alert-danger"><%= error %></div>
            <% } %>
            <form onsubmit="showLoading();" class="form-login" action="/login" method="POST">
                <div class="input-group mb-3">
                    <span class="input-icon"><i class="fas fa-user"></i></span>
                    <input type="text" id="username" name="username" placeholder="Usuário" required>
                </div>
                <div class="input-group mb-3">
                    <span class="input-icon"><i class="fas fa-lock"></i></span>
                    <input type="password" id="password" name="password" placeholder="Senha" required>
                </div>
                <div class="d-grid">
                    <button onclick="showLoading();" type="submit" class="login-button">ENTRAR</button>
                </div>
            </form>
            <p class="footer-text">© 2024 Seu Painel. Todos os direitos reservados.</p>
        </div>
    </div>
    <!-- Loading Overlay (opcional, pode ser ativado via JS) -->
    <div class="loading-overlay active">
        <div class="spinner"></div>
    </div>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script src="/js/main.js"></script>

</body>
</html>
